int main() {
    if(usr_id != ID){
      printf("hey\n");
      if(current_msg_id != msg_id){
        printf("me\n");
        printf("%s:%s", usr_name, msg);
        current_msg_id = msg_id;
      }
    }
}