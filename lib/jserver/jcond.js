//===================================================================
// This is the condition evaluator in JAM J side
//===================================================================

// Load some global modules

module.exports = new function() {
    this.init = init;
    this.reinit = reinit;
    this.checkCondition = checkCondition;
};


function init(mstate) {
    
}

function reinit(mstate) {
    
}

function checkCondition(condition) {
    
    return true;
}
