This is an older example.

Note: In the C side, the sending and receiving calls are not within an
asynchronous task. In the newer examples, we have them inside 
asynchronous tasks, which is the way to go.

