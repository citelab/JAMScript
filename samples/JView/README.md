# JView

# Setup
- Install **npm** and **node**

# Installation
We shall now install dependencies for **server**. `cd` to project directory. Then
```sh
$ npm install
```

# Run By
```sh
$ npm start
```