#include <stdio.h>
#include <unistd.h>

int main() {
	struct myType localStruct;
   	struct anotherType zzz;
   
	while (1) {
		sleep(1);
		localStruct = fruit;
	        zzz = qqq;
		printf("%d\n", y);
		printf("%d %f\n", localStruct.apple, localStruct.pear);
	        printf("%d %f %f %d\n", zzz.q, zzz.xx, zzz.yy, zzz.zz);
	   
	}
}
