#include <stdio.h>
#include <unistd.h>

int main() {
	struct myType localStruct;

	while (1) {
		usleep(10000);
		localStruct = fruit;
		printf("%d\n", y);
		printf("%d %f\n", localStruct.apple, localStruct.pear);
	}
}
