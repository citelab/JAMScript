#include <stdio.h>
#include <unistd.h>

int main() {
	struct myType localStruct;
	localStruct = fruit;

	while (1) {
		sleep(1);
		printf("%d\n", y);
		printf("%d %f\n", localStruct.apple, localStruct.pear);
	}
}
