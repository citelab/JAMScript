TODO

High precision timers could still collide if they are taken in different
machines. So we need to resolve this problem with activity serial numbers.


Update install instructions

sudo apt-get install xz-utils


sudo tar -C /usr/local --strip-components 1 -xJf node-v6.5.0-linux-x64.tar.xz
